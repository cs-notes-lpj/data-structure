/**
 * 链栈 <= 单链表 + 只允许在一端进行插入删除操作
*/

#include <stdio.h>

/* --- 定义数据类型：链栈的结点 --- */
struct NODE {
  int data;
  struct NODE * next;
};

typedef struct NODE LSNode;
typedef struct NODE * LinkStack;
/* --- 定义数据类型：链栈的结点 --- */

int main() {

  LinkStack s = NULL;

  return 0;
}