/*
  from《算法笔记》
  
  缘由：两个大整数做减法也可能导致超出整数的取值范围
*/

#include <stdio.h>
#include <string.h>

typedef struct {
  int num[1000]; // 为方便数组的顺序枚举，将整数的低位存储在数组的低位
  int len;
} bigN;

int main(void) {

  

  return 0;
}
